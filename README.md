# IT Pulse — Новости технологий

**IT Pulse** — это современная платформа для чтения новостей и ведения блогов о технологиях. Проект сочетает в себе минималистичный дизайн ("Zen-mode"), социальные функции и мощные инструменты на базе искусственного интеллекта (Google Gemini) для помощи авторам в написании статей.

![IT Pulse Preview](https://picsum.photos/800/400?grayscale)

## 🚀 Функциональность

### Читателю
*   **Умная лента**: Фильтрация по категориям (Разработка, ИИ, Дизайн, Стартапы, Железо).
*   **Поиск**: Мгновенный поиск по заголовкам и содержанию статей.
*   **Закладки**: Сохранение интересных статей для отложенного чтения.
*   **Режим фокусировки**: При чтении статьи скрываются боковые панели, чтобы ничто не отвлекало от контента.
*   **Социальные функции**: Лайки, комментарии, подписка на авторов.

### Автору
*   **AI-Ассистент**: Встроенный редактор использует **Google Gemini API** для:
    *   Рерайтинга и улучшения текста (исправление стиля, грамматики).
    *   Автоматической генерации краткого содержания (excerpt).
*   **Markdown**: Полная поддержка форматирования (код, списки, цитаты).
*   **Публикация**: Мгновенное добавление статей в общую ленту.

## 🛠 Технологический стек

*   **Frontend**: React 19, TypeScript
*   **Стилизация**: Tailwind CSS (CDN/Config)
*   **AI**: Google Gemini API (`@google/genai`)
*   **Иконки**: Lucide React
*   **Рендеринг текста**: React Markdown
*   **Хранение данных**: LocalStorage (эмуляция бекенда)

---

## ⚙️ Установка и запуск

Для запуска проекта локально вам понадобится установленный **Node.js**.

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/it-pulse.git
cd it-pulse
```

### 2. Установка зависимостей

Проект использует современные библиотеки. Если вы используете сборщик (например, Vite), убедитесь, что установлены следующие пакеты:

```bash
npm install react react-dom lucide-react react-markdown @google/genai
npm install -D tailwindcss postcss autoprefixer
```

### 3. Настройка API ключа (Важно!) 🔑

Для работы функций искусственного интеллекта (редактор статей) необходим ключ от **Google AI Studio**.

1.  Получите ключ на [aistudio.google.com](https://aistudio.google.com/).
2.  В корне проекта создайте файл `.env` (если используете Vite):

```env
VITE_API_KEY=ваш_ключ_от_gemini_api
```
*Примечание: Если вы используете другой сборщик, убедитесь, что переменная окружения `API_KEY` или `process.env.API_KEY` доступна в коде.*

### 4. Запуск

```bash
npm run dev
```

Откройте браузер по адресу `http://localhost:5173` (или другой порт, указанный в консоли).

---

## 📖 Как пользоваться

### Авторизация
1.  Нажмите кнопку **"Войти"** в левом меню (или иконку профиля на мобильном).
2.  Введите любое имя (система эмулирует вход, пароль не требуется).

### Чтение и взаимодействие
*   Используйте **Поиск** в правой колонке для фильтрации контента.
*   Нажмите на иконку **Закладки** внутри статьи, чтобы сохранить её.
*   Нажмите **"Обсудить"**, чтобы перейти к комментариям.

### Написание статьи с AI
1.  Перейдите в раздел **"Написать"**.
2.  Введите заголовок и набросайте черновик текста.
3.  Нажмите кнопку **"Улучшить текст с AI"** (иконка ✨).
4.  Нейросеть перепишет ваш текст, исправит ошибки и улучшит структуру.
5.  Нажмите **"Опубликовать"**.

---

## 📂 Структура проекта

```
src/
├── components/       # UI компоненты
│   ├── ArticleCard.tsx   # Карточка статьи в ленте
│   ├── ArticleDetail.tsx # Страница полного просмотра
│   ├── Editor.tsx        # Редактор с AI
│   ├── Feed.tsx          # Лента новостей
│   └── Layout.tsx        # Основной каркас (сайдбары, навигация)
├── services/
│   ├── authService.ts    # Управление пользователями
│   ├── geminiService.ts  # Интеграция с Google AI
│   └── storageService.ts # Локальная база данных (LS)
├── App.tsx           # Роутинг и управление состоянием
├── types.ts          # TypeScript интерфейсы
└── index.tsx         # Точка входа
```
